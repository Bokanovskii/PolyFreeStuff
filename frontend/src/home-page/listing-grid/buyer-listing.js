import React from 'react';

function Listing(props) {
    return (
        <td>
            { /* props.listing is a json object for a listing, returned in an array of all listings */ }
        </td>
    );
}

export default Listing;